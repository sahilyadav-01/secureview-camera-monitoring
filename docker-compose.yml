version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: secureview-postgres
    restart: always
    environment:
      POSTGRES_USER: secureview
      POSTGRES_PASSWORD: secureview_pass
      POSTGRES_DB: secureview_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U secureview -d secureview_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: secureview-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: secureview-backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DATABASE_URL=postgresql://secureview:secureview_pass@postgres:5432/secureview_db?schema=public
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=secureview-super-secret-jwt-key-change-in-production-2026
      - CORS_ORIGIN=http://localhost:3000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  monitoring-service:
    build:
      context: ./monitoring-service
      dockerfile: Dockerfile
    container_name: secureview-monitoring
    restart: always
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
      - DATABASE_URL=postgresql://secureview:secureview_pass@postgres:5432/secureview_db?schema=public
      - BACKEND_URL=http://backend:5000
    depends_on:
      - backend

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: secureview-frontend
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
  redis_data:
